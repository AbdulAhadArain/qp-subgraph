{
  "name": "test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:arbitrum": "graph build --network arbitrum-one",
    "build:base": "graph build --network base",
    "deploy:arbitrum": "graph deploy --node https://api.studio.thegraph.com/deploy/ qp-arbitrum",
    "deploy:base": "graph deploy --node https://api.studio.thegraph.com/deploy/ qp-base",
    "create-local": "graph create --node http://localhost:8020/ test",
    "remove-local": "graph remove --node http://localhost:8020/ test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.81.0",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0",
    "mustache": "^4.2.0"
  }
}
